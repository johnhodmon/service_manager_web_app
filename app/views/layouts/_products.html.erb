

<h3><%= link_to "Create New Product" , new_product_path %> </h3>
<ul class="nav nav-pills nav-stacked side-nav">

  <% @products.each do |product| %>
    
        <li role="presentation" class=<%= 'active' if params[:id].to_i==product.id %>><%= link_to raw('<h3>'+product.manufacturer.name+'</h3><p>'+product.product_number+'</p>'), product %></li>
    
        <% end %>

</ul>